[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "manylinux-baseline-probe"
version = "0.1.0"
requires-python = ">=3.7"
description = "Tiny native extension used to measure manylinux baseline floors"
